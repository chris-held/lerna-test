import { createTheme } from '@mui/material';
export { Button, Typography } from '@mui/material';

var theme = createTheme({
    palette: {
        primary: {
            main: "#03fc49"
        },
        secondary: {
            main: "#03cffc"
        }
    }
});

export { theme };
